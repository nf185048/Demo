import React from "react";
import type { FC } from "react";

export const DigitalOrdering: FC<{}> = ({ children }) => {
  return (
    <div>
      <p>Digital Ordering</p>
      {children}
    </div>
  );
};
